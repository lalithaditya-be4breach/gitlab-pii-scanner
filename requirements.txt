# ---------------------------------------------------------------------------
# GitLab PII Scanner - Dependencies
#
# Phase 1: foundation (config, logging, models, utils).
# Phase 2: Repository Manager (local validation + GitLab cloning).
# Phase 3 (current): File traversal + Microsoft Presidio integration.
#
# Later phases will extend this file with, e.g.:
#   - jinja2  (Phase 4: HTML report generation)
# ---------------------------------------------------------------------------

python-dotenv==1.0.1
GitPython==3.1.43

# -- Phase 3: PII detection --
presidio-analyzer==2.2.364
spacy>=3.4.4,!=3.7.0,<4.0.0

# -- Task 2, Phase 2: AI Assistant (optional) --
# Only required if AI_PROVIDER is set to "openai" or "azure_openai".
# The scanner runs fully without it (AI_PROVIDER defaults to "null",
# which uses a deterministic fallback summary and never imports this
# package).
openai>=1.30.0,<2.0.0

# -- Development / testing only --
pytest==8.3.3

# After installing, download the spaCy model Presidio uses by default
# (override with the PRESIDIO_SPACY_MODEL env var to use a different one,
# e.g. the smaller/faster en_core_web_sm):
#   python -m spacy download en_core_web_lg
